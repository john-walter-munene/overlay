name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  shared:
    name: Shared engine (stats + integrity)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # Pure-logic tests (stats, integrity, odds/results mappers, payout math)
      # run on Node's native TS type-stripping — no install needed.
      - name: Run unit tests
        run: node --experimental-strip-types --test packages/shared/src/*.test.ts apps/api/src/**/*.test.ts

  build:
    name: Install & build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install
      - run: npm run prisma:generate
      - run: npm run build --if-present
